  # Build section. It specifies the build process of the project
build:

  # Dependencies of the project
  deps:
    - dst: /usr/share/genesis_observability
      path:
        src: images/configs

  # List of elements in the project
  elements:
    # List of images in the element
    - images:
      - name: genesis-prometheus
        format: raw

        # OS profile for the image
        profile: ubuntu_24

        # Provisioning script
        script: images/prom.sh
      # manifest: manifests/genesis-core.yaml

        override:
          disk_size: "4G"
